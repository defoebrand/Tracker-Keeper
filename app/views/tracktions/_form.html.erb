<%= form_with(model: tracktion, local: true) do |form| %>
  <% if tracktion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tracktion.errors.count, "error") %> prohibited this tracktion from being saved:</h2>

      <ul>
        <% tracktion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.text_field :author_id %>
  </div>

  <div class="field">
    <%= form.label :type_id %>
    <%= form.text_field :type_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
